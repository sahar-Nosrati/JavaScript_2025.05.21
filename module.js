const additionNumbers = function (number1, number2) {
  const numbersSum = number1 + number2;
  return numbersSum;
};

const calculatedSum = additionNumbers(25, 16);

const lastName = "Maryam";

export{calculatedSum, additionNumbers};
export default lastName; 
